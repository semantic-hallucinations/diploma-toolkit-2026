# Compiler Roadmap

## Next Work

- Add a separate normal-control checker for overfull boxes, warnings, page
  layout, required sections, references, and other formatting risks. Keep this
  separate from the compiler so regular PDF builds stay simple.
